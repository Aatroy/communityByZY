<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.youyiwen.Mapper.UserMapper">
    <insert id="insertUser" parameterType="com.youyiwen.Bean.User">
        insert into user (`userName`,`password`,`fullName`,`telephone`,`city`,`job`,`gender`,`Email`)
        values (#{userName},#{password},#{fullName},#{telephone},#{city},#{job},#{gender},#{Email})
    </insert>
</mapper>
